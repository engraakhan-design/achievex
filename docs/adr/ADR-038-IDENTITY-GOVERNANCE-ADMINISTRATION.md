# ADR-038 — Identity Governance and Administration

Authentication/provisioning remain separate from certification. Review decisions and SoD exceptions are append-only records. Revocation decisions are recommendations until a configured provisioning adapter executes them. Privileged assignments are time-bound and break-glass usage is explicit.
