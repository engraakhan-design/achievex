# Integration Automation Builder

Sprint 18.4 adds versioned workflow graphs, manual/scheduled/event/webhook/data-sync triggers, typed nodes and edges, idempotent durable runs, per-step retries, approvals, cancellation, analytics, REST administration, and a no-code builder UI foundation.

Worker adapters execute external effects. Published definitions contain references and safe configuration only; secrets remain in governed credential stores.
