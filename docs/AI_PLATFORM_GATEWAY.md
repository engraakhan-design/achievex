# AI Platform Gateway

Sprint 16.1 adds provider/model registries, prompt versioning, deterministic adapter contracts, execution routing, usage accounting, cost estimation, and execution audit. The included OpenAI-compatible adapter is a safe foundation stub; production transports must implement authenticated HTTP calls, retry, timeout, streaming and provider-specific safety handling.
