import { DeveloperPlatformService } from './developer-platform.service';
describe('DeveloperPlatformService',()=>{it('publishes an OpenAPI 3.1 manifest without credentials',()=>{const service=new DeveloperPlatformService({} as any);const manifest=service.openApiManifest();expect(manifest.openapi).toBe('3.1.0');expect(JSON.stringify(manifest)).not.toContain('secretHash')});it('returns an SDK generation plan rather than executing processes',()=>{const service=new DeveloperPlatformService({} as any);expect(service.generateSdk({language:'typescript'}).status).toBe('generation-plan')})})
