import { WorkspaceCommunityService } from './workspace-community.service';
describe('WorkspaceCommunityService',()=>{it('is constructible',()=>{expect(new WorkspaceCommunityService({} as never)).toBeDefined();});});
