import { DocumentAuthoringService } from './document-authoring.service';
describe('DocumentAuthoringService',()=>{it('is defined',()=>{expect(new DocumentAuthoringService({} as never,{} as never)).toBeDefined();});});
