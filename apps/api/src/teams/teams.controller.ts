import {Body,Controller,Delete,Get,Param,Patch,Post,Put} from '@nestjs/common';import {ApiBearerAuth,ApiTags} from '@nestjs/swagger';import {AuthenticatedUser} from '../auth/auth.types';import {CurrentUser} from '../auth/decorators/current-user.decorator';import {RequirePermissions} from '../auth/decorators/permissions.decorator';import {CreateTeamDto,UpdateTeamDto,UpdateTeamMembersDto} from './dto/teams.dto';import {TeamsService} from './teams.service';
@ApiTags('teams') @ApiBearerAuth() @Controller('teams') export class TeamsController{constructor(private s:TeamsService){} @Get() @RequirePermissions('users.read') list(@CurrentUser()u:AuthenticatedUser){return this.s.list(u.organizationId)} @Post() @RequirePermissions('organization.manage') create(@CurrentUser()u:AuthenticatedUser,@Body()d:CreateTeamDto){return this.s.create(u.organizationId,u.sub,d)} @Patch(':id') @RequirePermissions('organization.manage') update(@CurrentUser()u:AuthenticatedUser,@Param('id')id:string,@Body()d:UpdateTeamDto){return this.s.update(u.organizationId,id,u.sub,d)} @Put(':id/members') @RequirePermissions('users.manage') members(@CurrentUser()u:AuthenticatedUser,@Param('id')id:string,@Body()d:UpdateTeamMembersDto){return this.s.members(u.organizationId,id,u.sub,d)} @Delete(':id') @RequirePermissions('organization.manage') remove(@CurrentUser()u:AuthenticatedUser,@Param('id')id:string){return this.s.remove(u.organizationId,id,u.sub)}}
