'use client';
import { FormEvent, useEffect, useState } from 'react';
import { AdminShell } from '../../components/admin-shell'; import { api } from '../../lib/api';
type Cycle={id:string;name:string;description?:string;startDate:string;endDate:string;status:string;isDefault:boolean;_count:{objectives:number}};
export default function Cycles(){const [items,setItems]=useState<Cycle[]>([]),[error,setError]=useState('');const [form,setForm]=useState({name:'Q3 2026',startDate:'2026-07-01',endDate:'2026-09-30',status:'ACTIVE',isDefault:true});async function load(){try{setItems(await api<Cycle[]>('/okrs/cycles'))}catch(e){setError((e as Error).message)}}useEffect(()=>{void load()},[]);async function submit(e:FormEvent){e.preventDefault();try{await api('/okrs/cycles',{method:'POST',body:JSON.stringify(form)});await load()}catch(e){setError((e as Error).message)}}return <AdminShell title="OKR cycles" subtitle="Define the planning periods used across your organization."><form className="panel inlineForm cycleForm" onSubmit={submit}><input placeholder="Cycle name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input type="date" value={form.startDate} onChange={e=>setForm({...form,startDate:e.target.value})}/><input type="date" value={form.endDate} onChange={e=>setForm({...form,endDate:e.target.value})}/><select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option>ACTIVE</option><option>DRAFT</option><option>CLOSED</option></select><button>Create cycle</button></form>{error&&<p className="formError">{error}</p>}<div className="cardGrid">{items.map(x=><article className="panel entityCard" key={x.id}><div className="entityIcon">{x.name.slice(0,2)}</div><h2>{x.name}</h2><p>{new Date(x.startDate).toLocaleDateString()} – {new Date(x.endDate).toLocaleDateString()}</p><div><span>{x.status}{x.isDefault?' · Default':''}</span><span>{x._count.objectives} objectives</span></div></article>)}</div></AdminShell>}
