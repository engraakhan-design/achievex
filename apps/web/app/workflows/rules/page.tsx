export default function RulesPage(){return <main className="space-y-6"><header><h1 className="text-2xl font-semibold">Business Rules</h1><p className="text-sm text-slate-500">Create, version, publish, dry-run, and audit tenant-scoped business rules.</p></header><section className="grid gap-4 md:grid-cols-3">{['Published rules','Draft versions','Recent executions'].map(x=><div key={x} className="rounded-xl border bg-white p-5"><h2 className="font-medium">{x}</h2><p className="mt-2 text-sm text-slate-500">Connect this card to the rules API.</p></div>)}</section><section className="rounded-xl border bg-white p-5"><h2 className="font-medium">Supported actions</h2><p className="mt-2 text-sm text-slate-500">Create task, notify, start workflow, assign user, update field, invoke AI, call webhook, and emit event.</p></section></main>}