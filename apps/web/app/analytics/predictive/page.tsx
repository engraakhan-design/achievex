import { AdminShell } from '../../../components/admin-shell';
const useCases=[['OKR achievement','Forecast likely objective attainment from historical progress'],['Project completion','Project delivery dates using transparent trend models'],['Resource shortage','Capacity demand and shortage signals'],['Budget overrun','Financial exposure based on observed variance'],['Delivery risk','Weighted risk indicators with auditable factors'],['SLA breach','Probability signals from response and completion history']];
export default function PredictiveAnalyticsPage(){return <AdminShell title="Predictive analytics" subtitle="Transparent forecasts with confidence scores, evidence, and governed model versions."><section className="grid">{useCases.map(([name,desc])=><article className="card" key={name}><p className="eyebrow">Prediction use case</p><h2>{name}</h2><p>{desc}</p></article>)}</section><section className="card"><h2>Model governance</h2><p>Published model versions are immutable. Every prediction records its exact version, input features, confidence score, horizon, and explanation. Dry runs do not persist prediction results.</p></section><section className="card"><h2>Initial model catalogue</h2><p>Linear trend, moving average, exponential smoothing, and weighted logistic risk scoring. Advanced machine-learning providers can be added later behind the same governed contract.</p></section></AdminShell>}
