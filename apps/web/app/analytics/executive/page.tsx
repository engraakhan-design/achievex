import Link from 'next/link';
import { AdminShell } from '../../../components/admin-shell';
const roles=[['CEO','Strategy, enterprise health and portfolio outcomes'],['COO','Operations, delivery, SLA and automation'],['CFO','Value, budget variance and financial exposure'],['CHRO','Capacity, utilization, skills and hiring signals'],['PMO','Portfolio health, milestones and delivery risk'],['DEPARTMENT_HEAD','Department OKRs, capacity and execution']];
export default function ExecutiveDashboardsPage(){return <AdminShell title="Executive dashboards" subtitle="Role-based decision views with reusable KPI widgets and drill-down paths."><section className="grid">{roles.map(([role,desc])=><article className="card" key={role}><p className="eyebrow">{role.replace('_',' ')}</p><h2>{role.replace('_',' ')} scorecard</h2><p>{desc}</p><Link href={`/analytics/dashboards?audience=${role}`}>Open dashboard</Link></article>)}</section><section className="card"><h2>Dashboard governance</h2><p>Templates are tenant-scoped, metrics are versioned, saved views retain filters, and widgets resolve values from immutable metric snapshots.</p></section></AdminShell>}
