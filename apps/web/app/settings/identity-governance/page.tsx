const cards=[['Access certification','Campaigns, reviewer queues, due dates and immutable decisions.'],['Segregation of duties','Conflict rules, violations, risk severity and time-bound exceptions.'],['Privileged access','Time-bound and break-glass assignments with approval and expiry.'],['Access requests','Multi-stage request and approval records with provisioning status.'],['Role mining','Schema boundary for least-privilege recommendations and role cleanup.'],['Identity analytics','Campaign completion, violations, dormant access and privileged assignments.']]; export default function Page(){return <main><h1>Identity governance</h1><p>Review, certify and govern enterprise access without automatically applying destructive entitlement changes.</p><section>{cards.map(([h,b])=><article key={h}><h2>{h}</h2><p>{b}</p></article>)}</section></main>}
