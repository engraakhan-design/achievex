export default function AutomationBuilderPage(){const cards=[['Workflow canvas','Connect triggers, conditions, actions, delays, approvals, connector operations, and webhooks as a governed graph.'],['Trigger catalog','Start automations manually, on schedules, platform events, incoming webhooks, or completed data synchronizations.'],['Human approval','Pause sensitive actions for independent approval and preserve the request, decision, notes, and identity trail.'],['Reliable runs','Track every run and step with idempotency, correlation IDs, retry policies, timeouts, failures, and cancellation.'],['Integration actions','Invoke governed connector, webhook, public API, collaboration, document, and notification operations through worker adapters.'],['Operations','Review pending approvals, failed steps, retry queues, run history, and 90-day execution analytics.']];return <main style={{padding:32,maxWidth:1200,margin:'0 auto'}}><h1>No-Code Automation Builder</h1><p>Design and operate tenant-governed enterprise automations without embedding provider credentials or execution logic in the visual definition.</p><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(280px,1fr))',gap:16}}>{cards.map(([t,d])=><section key={t} style={{border:'1px solid #ddd',borderRadius:12,padding:20}}><h2>{t}</h2><p>{d}</p></section>)}</div><h2>Execution boundary</h2><p>Published definitions are immutable. Distributed workers must claim pending steps, enforce connector policies, apply retries and idempotency, and report outcomes through the worker API.</p></main>}
