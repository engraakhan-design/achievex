import { AdminShell } from '../../../components/admin-shell';
const cards=[['Single sign-on','Configure governed SAML 2.0 and OpenID Connect providers with verified domains and just-in-time provisioning.'],['Directory connections','Connect Microsoft Entra ID, Okta, Google Workspace, or a generic SCIM directory.'],['Attribute mappings','Map external identity attributes and groups to AchieveX users, roles, departments, and teams.'],['Provisioning monitor','Review synchronization runs, create/update/suspend actions, failures, and correlation identifiers.']];
export default function IdentityPage(){return <AdminShell title="Identity & provisioning" subtitle="Federated authentication and governed workforce lifecycle synchronization."><div className="grid">{cards.map(([t,d])=><article className="card" key={t}><h2>{t}</h2><p>{d}</p></article>)}</div><section className="card"><h2>Security boundaries</h2><p>Secrets are write-only, tenant scoped, fingerprinted, and must be backed by KMS in production. Preview runs are side-effect free. Deprovisioning is disabled by default.</p></section></AdminShell>}
