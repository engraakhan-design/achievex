import { AdminShell } from '../../../components/admin-shell';
const cards=[['Connector catalog','Govern approved HRIS, CRM, ERP, project, collaboration, productivity, and custom adapters.'],['Secure connections','Manage tenant-scoped endpoints, encrypted credentials, health state, and credential rotation.'],['Synchronization definitions','Configure source objects, AchieveX targets, direction, schedules, checkpoints, filters, and conflict policy.'],['Field mapping','Map source fields to target fields with transformations, required-field controls, defaults, and sensitive-data markers.'],['Run operations','Inspect durable run history, record outcomes, checkpoints, errors, and correlation identifiers.'],['Conflict center','Resolve bidirectional data conflicts using source-wins, target-wins, latest-wins, or manual decisions.']];
export default function DataSyncSettingsPage(){return <AdminShell title="Enterprise data synchronization" subtitle="Govern external data movement without duplicating provider-specific adapters."><section className="grid">{cards.map(([t,d])=><article className="card" key={t}><p className="eyebrow">Data synchronization</p><h2>{t}</h2><p>{d}</p></article>)}</section><section className="card"><h2>Operational boundary</h2><p>Run creation is durable in the API. Provider calls, distributed scheduling, locking, throttling, retries, and high-volume record processing belong in connector workers.</p></section></AdminShell>}
