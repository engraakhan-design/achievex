import { AdminShell } from '../../../components/admin-shell';
const cards=[['Sources','Register OKRs, projects, policies, files and connector content.'],['Documents','Inspect normalized documents, versions and visibility.'],['Index jobs','Monitor chunking, embedding generation and failures.'],['Retrieval','Test permission-aware hybrid search and citations.'],['Analytics','Review source freshness, retrieval quality and latency.'],['Settings','Configure chunk size, overlap, ranking and retention.']];
export default function KnowledgePlatformPage(){return <AdminShell title="Enterprise Knowledge Platform" subtitle="Tenant-isolated retrieval, indexing and provenance for grounded AI."><section className="grid">{cards.map(([t,d])=><article className="card" key={t}><p className="eyebrow">Knowledge</p><h2>{t}</h2><p>{d}</p></article>)}</section></AdminShell>}
