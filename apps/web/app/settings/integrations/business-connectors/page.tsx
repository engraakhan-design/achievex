import { AdminShell } from '../../../../components/admin-shell';
const providers=[['Jira','Projects, epics, stories, bugs and issues'],['Azure DevOps','Projects and work items'],['GitHub','Repositories, issues and pull requests'],['GitLab','Projects, issues and merge requests'],['ServiceNow','Incidents, changes and tasks'],['Asana','Projects and tasks'],['Monday.com','Boards and items'],['Trello','Boards and cards']];
export default function Page(){return <AdminShell title="Business connectors" subtitle="Governed native adapters for project, engineering and service-management systems."><section className="grid">{providers.map(([n,d])=><article className="card" key={n}><p className="eyebrow">Native connector</p><h2>{n}</h2><p>{d}</p></article>)}</section><section className="card"><h2>Synchronization governance</h2><p>Preview runs are side-effect free. Field mappings are explicit, sync items are idempotent by external identity, and conflicting changes require the configured resolution strategy or human review.</p></section></AdminShell>}
