import { AdminShell } from '../../../components/admin-shell';
const capabilities=[['Installed connectors','Manage tenant connector instances, versions, configuration and lifecycle.'],['Connection health','Review latest health checks, latency, success rate, retries and dead-letter counts.'],['Secure credentials','Store masked tenant credentials with rotation and expiration metadata.'],['Execution history','Inspect tests, syncs, pulls, pushes and webhook processing by correlation ID.']];
export default function IntegrationsPage(){return <AdminShell title="Enterprise integrations" subtitle="Operate the connector framework, event bus, webhooks and integration health."><section className="grid">{capabilities.map(([n,d])=><article className="card" key={n}><p className="eyebrow">Integration platform</p><h2>{n}</h2><p>{d}</p></article>)}</section><section className="card"><h2>Security boundary</h2><p>Credential payloads are write-only through management APIs. Read models expose fingerprints and expiry metadata, never secret values.</p></section></AdminShell>}
