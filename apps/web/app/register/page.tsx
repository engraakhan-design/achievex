'use client';
import Link from 'next/link'; import { FormEvent,useState } from 'react'; import { api } from '../../lib/api';
export default function RegisterPage(){const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const fd=new FormData(e.currentTarget);try{const data=await api<{tokens:{accessToken:string;refreshToken:string}}>('/auth/register',{method:'POST',body:JSON.stringify(Object.fromEntries(fd))});localStorage.setItem('achievex_access_token',data.tokens.accessToken);localStorage.setItem('achievex_refresh_token',data.tokens.refreshToken);location.href='/';}catch(err){setError(err instanceof Error?err.message:'Registration failed')}finally{setLoading(false)}}return <main className="authPage"><section className="authCard wide"><div className="authBrand"><span>A</span>AchieveX</div><p className="eyebrow">Start your workspace</p><h1>Create your organization</h1><form onSubmit={submit}><div className="formGrid"><label>Organization name<input name="organizationName" required /></label><label>Workspace slug<input name="organizationSlug" required pattern="[a-z0-9-]+" placeholder="acme" /></label><label>First name<input name="firstName" required /></label><label>Last name<input name="lastName" required /></label></div><label>Work email<input name="email" type="email" required /></label><label>Password<input name="password" type="password" required minLength={10} /></label>{error&&<div className="formError">{error}</div>}<button disabled={loading}>{loading?'Creating…':'Create workspace'}</button></form><p className="authFooter">Already registered? <Link href="/login">Sign in</Link></p></section></main>}
