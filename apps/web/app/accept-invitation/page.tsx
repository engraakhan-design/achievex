'use client';
import { FormEvent,useState } from 'react'; import { api } from '../../lib/api';
export default function AcceptInvitation(){const[message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);try{await api('/auth/invitations/accept',{method:'POST',body:JSON.stringify(Object.fromEntries(fd))});setMessage('Invitation accepted. You can now sign in.')}catch(err){setMessage(err instanceof Error?err.message:'Unable to accept invitation')}}return <main className="authPage"><section className="authCard"><div className="authBrand"><span>A</span>AchieveX</div><h1>Accept your invitation</h1><form onSubmit={submit}><label>Invitation token<input name="token" defaultValue={typeof window!=='undefined'?new URLSearchParams(location.search).get('token')??'':''} required /></label><label>Create password<input name="password" type="password" minLength={10} required /></label>{message&&<div className="formNotice">{message}</div>}<button>Activate account</button></form></section></main>}
